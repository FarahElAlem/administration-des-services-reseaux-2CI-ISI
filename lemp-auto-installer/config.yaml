# ═══════════════════════════════════════════════════════════
# Configuration LEMP Auto-Installer v2.0
# Modifiez ce fichier AVANT de lancer install.sh
# ═══════════════════════════════════════════════════════════

# Informations Personnelles
user:
  firstname: "Farah"
  lastname: "EL ALEM"
  formation: "ISGA Marrakech"
  email: "farah.el1996@gmail.com"

# Configuration Serveur
server:
  hostname: "srv-web01"
  ip: "192.168.1.50"
  interface: "ens37"
  timezone: "Africa/Casablanca"

# Virtual Hosts
vhosts:
  portal_rh:
    enabled: true
    domain: "portal-rh.ing-infraFarah.lan"
    type: "php"
    root: "/var/www/portal-rh.ing-infraFarah.lan/html"
    
  prod_web:
    enabled: true
    domain: "prod-web.innov-techFarah.com"
    type: "static"
    root: "/var/www/prod-web.innov-techFarah.com/html"
  # NOUVEAU DOMAINE
  blog:
    enabled: true
    domain: "blog.innov-techFarah.com"
    type: "php"  # ou "static"
    root: "/var/www/blog.innov-techFarah.com/html"

# Sécurité (CHANGEZ CES MOTS DE PASSE EN PRODUCTION !)
security:
  mariadb_root_password: "MariaDB2026!"
 # phpmyadmin_password: "phpMyAdmin2027!"

# Composants à installer
components:
  nginx: true
  mariadb: true
  php_fpm: true
  phpmyadmin: true
  fail2ban: true
  ufw: false

# Options
options:
  create_backup: true
  generate_hosts_file: true
  run_tests: true
  verbose: true
